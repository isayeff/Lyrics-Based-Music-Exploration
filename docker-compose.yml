services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: diss
      POSTGRES_PASSWORD: diss
      POSTGRES_DB: music
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata: